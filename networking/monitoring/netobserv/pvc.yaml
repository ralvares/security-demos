apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loki-store
  namespace: netobserv
spec:
  resources:
    requests:
      storage: 10G
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
